BUGS:

To-do:
	
## Pending — Next Revision Pass

1. **"special" economy value** — standardize to `special_immediate` or keep as a distinct value

2. **brawler, tactician** — populate stubs when ready

3. **Full type/tag audit across careerDataMap**

4. **ancestryTalentDataMap tag normalization** — 115 of 160 talents use display-name tags instead of snake_case (e.g. `"Observation"` instead of `"observation"`, `"Behavioral Echo"` instead of `"behavioral_echo"`)

5. **Sheet worker — tracker strain deduction** — 674 trackable scene/session checkboxes across career talents and ancestry traits/talents

6. **Long Rest reset mechanism** — needed to properly support talents like `keeper_of_the_last_fire` (Draevi) that reset on Long Rest rather than scene or session. Currently set to `""` as a placeholder until this is implemented.

7. **Add Ancestry Traits to Summary Text and Summary Type**
   

Clean-up:

	Move Skill rolls to sheet worker on click so one place to edit the roll value.

Questions:

Wishlist:
