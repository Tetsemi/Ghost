BUGS:

To-do:
	
## Pending — Next Revision Pass

1. **"special" economy value** — standardize to `special_immediate` or keep as a distinct value

2. **brawler, tactician** — populate stubs when ready

3. **Full type/tag audit across all DataMaps**

4. **ancestryTalentDataMap tag normalization** — 115 of 160 talents use display-name tags instead of snake_case (e.g. `"Observation"` instead of `"observation"`, `"Behavioral Echo"` instead of `"behavioral_echo"`)

5. **Add Ancestry Traits to Summary Text**

6. **Merge ancestryTalentDataMap into ancestryDataMap**

7. Extended Magazines and Drums,
	Loadout items purchased separately from the weapon. Carry as many as you like. Choose what to load before the mission. Swapping mid-combat costs a Maneuver.
	
8. Weapon Traits should just be a readonly field populated by weaponDataMap of the weapon traits.

Clean-up:

	Move Skill rolls to sheet worker on click so one place to edit the roll value.

Questions:

Wishlist:
