BUGS:

To-do:
	
## Pending — Next Revision Pass

1. **"special" economy value** — standardize to `special_immediate` or keep as a distinct value

2. **brawler, tactician** — populate stubs when ready

3. **Full type/tag audit across all DataMaps**

4. **ancestryTalentDataMap tag normalization** — 115 of 160 talents use display-name tags instead of snake_case (e.g. `"Observation"` instead of `"observation"`, `"Behavioral Echo"` instead of `"behavioral_echo"`)

5. **Add Ancestry Traits to Summary Text**

6. **Merge ancestryTalentDataMap into ancestryDataMap**

7. Add checks so roll doesn't success if checkboxes don't have enough

8. Make sure the right Ammo and Mods are shown for the type of gun.

Remove checkbox, toggle by selecting ammo on/off.

And also shouldn't the Cap X/Y be the inverse of the checkboxes? As you check the checkboxes off, you should have less Cap remaining.

So start 5/5, hit FA, mark 3 checkboxes, and it should show 2/5 remaining, which means you need to switch to BF/SA/SS or reload for your next shot if you want FA on.  In fact, if you're in FA mode and it's showing 2/5 or 2 or less checkboxes left, it should unselect and grey out FA until the player reloads or picks another mode.  Same with BF and 1 checkbox remaining.

Extended Magazines and Drums,
Loadout items purchased separately from the weapon. Carry as many as you like. Choose what to load before the mission. Swapping mid-combat costs a Maneuver.

Clean-up:

	Move Skill rolls to sheet worker on click so one place to edit the roll value.

Questions:

Wishlist:
