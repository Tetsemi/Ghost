BUGS:
	
To-do:

	Add Career Primary Skill Limiter (4 choices), if career doesn't have exclusive skill set. (Pilot has 5 primary skills, only 4 should be allowed to be picked).

	Add HTML/CSS for Career: Operator
	Add HTML/CSS for Career: Pilot
	Add HTML/CSS for Career: Shadowblade
	Add HTML/CSS for Career: Slicer

	Add totals for equipped armor (Physical / Arcane )

		
	Combat:
		Add Traits Field for Weapons
		
Clean-up:

	Move Skill rolls to sheet worker on click so one place to edit the roll value.
	
	Background tab code needs to be unified like racial talent and career talent tabs
	
const registerCareerTabWatchers = () => {
  if (debug_on) console.log ("Object.keys(careerDataMap) loaded");
  if (debug_on_trace) console.log ("[Object.keys(careerDataMap)] Start");
  if (careerTabWatchersReady) return;
  careerTabWatchersReady = true;

  Object.keys(careerDataMap).forEach(career => {
    on(`clicked:${career}`, () => {
      if (debug_on_trace) console.log(`[career tab click] ${career}`);
      setAttrs({ selected_career: career });
    });
  });
};


Wishlist:


	

