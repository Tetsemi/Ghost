1. Update SCRIPT with career data - careerDataMap
2. Update JSON with career data - translation.json
2. Update HTML Career section tabs
3. Update CSS Career section tabs
4. Update HTML Tracker section tabs
5. update CSS Tracker section tabs

Recommended format for a single request (what you ask me for each tier):

“Next career: <career>; Tier <N>; here are the talent names + rules + prereqs + usage_limit + cost; generate:

	careerDataMap.<career>.talents entries (JS object)

	<div class="sheet-career-talent-tier" data-tier="<N>"> HTML block
	
	Include _enabled fields for Tier 2–5, capstone class for Tier 5, and use existing Investigator/Icon patterns.”

“Next career: spirit_warden; Tier 1; here are the talent names + rules + prereqs + usage_limit + cost; generate:
	careerDataMap.spirit_warden.talents entries (JS object)

	<div class="sheet-career-talent-tier" data-tier="1"> HTML block

	Include _enabled fields for Tier 2–5, capstone class for Tier 5, and use existing Investigator/Icon patterns.”
	