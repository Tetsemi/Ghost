// Simple template — replicate the inner blocks for each existing race and background.
// Races observed: alteri, draevi, feran, human, khadra, kitsu, lyranni, veyra.

const backgroundDataMap = {
  <race_key>: {
    <background_key>: {
      // Array of skill keys (length varies in your data)
      trained_skills: [
        // "stealth", "streetwise", "insight", ...
      ],

      // Either a single item i18n key string...
      // starting_item: "mask_journal-u",

      // ...or an array of item i18n key strings
      // starting_item: ["trusted_toolset-u", "modded_wearable-u"],
      starting_item: "",

      // Exactly three talent keys
      starting_talents: [
        // "quick_fixer", "hard_lesson", "skilled_focus"
      ]
    },

    // ...add additional existing background keys here...
  },

  // ...add additional existing race keys here...
};
