// Simple template — replicate the inner block per existing flaw key.
// Use your real keys (e.g., addiction_major, hunted, mental_scar, veil_touched, etc.)

const flawDataMap = {
  <flaw_key>: {
    // i18n keys from translation.json
    name: "flaw_<flaw_key>-u",
    flavor: "flaw_<flaw_key>_flavor-u",
    description: "flaw_<flaw_key>_description-u",

    // numeric cost (examples in existing data: 5, 10)
    cost: 0,

    // usage gating:
    //   ""         → no special limit
    //   "scene"    → once per scene (as used by entries like soul_flare)
    //   "session"  → once per session (as used by entries like hunted/wanted_low_priority)
    usage_limit: ""
  },

  // ...add additional existing flaw keys here...
};
