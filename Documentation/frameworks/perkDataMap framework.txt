// Simple template — replicate the inner block per perk key you already have.
// Use your existing keys like blood_born_survivor, combat_veteran, etc.

const perkDataMap = {
  "<perk_key>": {
    // i18n keys from translation.json
    name: "perk_<perk_key>-u",
    flavor: "perk_<perk_key>_flavor-u",
    description: "perk_<perk_key>_description-u",

    // numeric XP cost (examples in existing data: 10, 15, etc.)
    cost: 0,

    // usage gating:
    //   ""                      → no special limit
    //   "session"              → per-session
    //   ["scene", "session"]   → available once per scene and once per session
    usage_limit: ""
  },

  // ... add additional existing perk keys here ...
};
