Populate Racial Backgrounds and maodifiers - Framework in place
(Draevi Done)

Populate Racial Talents and modifiers - Framework in place

Modify skills to use dice modifier
<td><button class='new-roll' type='roll' value='&{template:coc-1} {{name=@{archery_txt}}} {{modifier=@{dice_modifier_checkbox}}} {{success=[[@{archery_mdr}+@{dice_modifier_checkbox}]]}} {{hard=[[floor((@{archery_mdr}+@{dice_modifier_checkbox})/2)]]}} {{extreme=[[floor((@{archery_mdr}+@{dice_modifier_checkbox})/5)]]}} {{roll1=[[1d100]]}}' name='roll_archery_mdr_check' /></td>

Look into mouse over descriptions for skills
https://jsfiddle.net/Lithl/4naLge77/

Update Combat section to use new names for Combat skills (Firearms(HG) becomes Pistol)

Remove Sanity

Update colors


Change this into functions

			streetwise_mdr: istreetwise && italent_streetwise ? istreetwise_skill + istreetwise_bonus + italent_streetwise_bonus : istreetwise ? istreetwise_skill + istreetwise_bonus : italent_streetwise ? istreetwise_skill + italent_streetwise_bonus : istreetwise_skill
					
function calcSkillWithBonuses(skill, bonus = 0, talentBonus = 0, hasBonus = false, hasTalent = false) {
	return hasBonus && hasTalent
		? skill + bonus + talentBonus
		: hasBonus
		? skill + bonus
		: hasTalent
		? skill + talentBonus
		: skill;
}

function buildSkillObject(skills) {
	const obj = {};
	skills.forEach(skill => {
		const skillVal = parseInt(skill.skill) || 0;
		const bonusVal = parseInt(skill.bonus) || 0;
		const talentVal = parseInt(skill.talentBonus) || 0;
		obj[`${skill.name}_mdr`] = calcSkillWithBonuses(
			skillVal,
			bonusVal,
			talentVal,
			skill.hasBonus,
			skill.hasTalent
		);
	});
	return obj;
}

getAttrs([
  "streetwise_skill_mdr", "draevi_streetwise_bonus_mdr", "draevi_talent_streetwise_bonus_mdr",
  "athletics_skill_mdr", "draevi_athletics_bonus_mdr",
  "draevi_mdr_checkbox", "draevi_scavengers_edge_mdr_checkbox"
], function(values) {
	const skills = [
		{
			name: "streetwise",
			skill: values.streetwise_skill_mdr,
			bonus: values.draevi_streetwise_bonus_mdr,
			talentBonus: values.draevi_talent_streetwise_bonus_mdr,
			hasBonus: values.draevi_mdr_checkbox === "streetwise",
			hasTalent: values.draevi_scavengers_edge_mdr_checkbox === "talent_streetwise"
		},
		{
			name: "athletics",
			skill: values.athletics_skill_mdr,
			bonus: values.draevi_athletics_bonus_mdr,
			talentBonus: 0, // no talent
			hasBonus: values.draevi_mdr_checkbox === "athletics",
			hasTalent: false
		}
	];

	const setObj = buildSkillObject(skills);
	setAttrs(setObj);
});